# run-json-schema-to-grammar.mjs 文档

主题：Node 脚本将 JSON Schema 转 Grammar 的 CLI 工具。

- 使用
  - `node tests/run-json-schema-to-grammar.mjs schema.json` 输出 grammar；
  - 解析引用、格式化输出。

- 验证
  - 以复杂 schema 测试，观察输出与期望一致性。
